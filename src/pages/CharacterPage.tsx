import { CharacterDetails, Header, TopProgressBar } from '../components';

export const CharacterPage = () => {
	return (
		<>
			<TopProgressBar />
			<Header />
			<CharacterDetails />
		</>
	);
};
